import { writable } from 'svelte/store';

export const url = writable('');
export const liverName = writable('');
export const liverIconUrl = writable('');
export const startedAt = writable('');
export const description = writable('');
